---
"date": "2025-04-15"
"description": "Μάθετε πώς να δημιουργείτε και να προσαρμόζετε γραφήματα χρησιμοποιώντας το Aspose.Slides για .NET, συμπεριλαμβανομένης της εμφάνισης ποσοστών ως ετικέτες δεδομένων. Ακολουθήστε αυτόν τον οδηγό βήμα προς βήμα."
"title": "Πώς να δημιουργήσετε και να προσαρμόσετε γραφήματα με το Aspose.Slides .NET® Εμφάνιση ποσοστών ως ετικέτες"
"url": "/el/net/charts-graphs/create-customize-charts-aspose-slides-dotnet/"
"weight": 1
---

{{< blocks/products/pf/main-wrap-class >}}

{{< blocks/products/pf/main-container >}}

{{< blocks/products/pf/tutorial-page-section >}}
# Πώς να δημιουργήσετε και να προσαρμόσετε γραφήματα με το Aspose.Slides .NET: Εμφάνιση ποσοστών ως ετικέτες

## Εισαγωγή

Η αποτελεσματική παρουσίαση δεδομένων είναι ζωτικής σημασίας σε πολλούς τομείς και τα γραφήματα παίζουν ζωτικό ρόλο μετατρέποντας σύνθετες πληροφορίες σε καθαρά γραφήματα. Η δημιουργία του τέλειου γραφήματος περιλαμβάνει εργασίες προσαρμογής, όπως η εμφάνιση ποσοστών σε ετικέτες—μια εργασία που γίνεται ευκολότερη με το Aspose.Slides για .NET. Αυτή η βιβλιοθήκη απλοποιεί τη διαδικασία δημιουργίας και τροποποίησης γραφημάτων σε παρουσιάσεις PowerPoint.

Σε αυτό το σεμινάριο, θα μάθετε πώς να χρησιμοποιείτε το Aspose.Slides για .NET για να δημιουργήσετε ένα γράφημα σωρευμένων στηλών από την αρχή και να το προσαρμόσετε εμφανίζοντας ποσοστιαίες τιμές ως ετικέτες δεδομένων. Ακολουθώντας αυτά τα βήματα, θα βελτιώσετε τις διαφάνειές σας με ακριβείς και οπτικά ελκυστικές αναπαραστάσεις δεδομένων.

**Τι θα μάθετε:**
- Αρχικοποίηση του Aspose.Slides για .NET
- Δημιουργία γραφήματος σωρευμένων στηλών
- Υπολογισμός και εμφάνιση ποσοστών σε ετικέτες δεδομένων
- Βέλτιστες πρακτικές βελτιστοποίησης της απόδοσης των γραφημάτων

Πριν προχωρήσουμε στην υλοποίηση, ας βεβαιωθούμε ότι έχετε όλα τα απαραίτητα για να ξεκινήσετε.

## Προαπαιτούμενα

Για να ακολουθήσετε αποτελεσματικά αυτό το σεμινάριο, βεβαιωθείτε ότι έχετε:
- **SDK πυρήνα .NET** εγκατεστημένο στο μηχάνημά σας.
- Βασική κατανόηση της ανάπτυξης εφαρμογών C# και .NET.
- Visual Studio ή παρόμοιο IDE για τη σύνταξη και εκτέλεση κώδικα C#.

Θα χρειαστείτε το Aspose.Slides για .NET για να δημιουργήσετε γραφήματα, οπότε βεβαιωθείτε ότι έχει ρυθμιστεί όπως περιγράφεται παρακάτω.

## Ρύθμιση του Aspose.Slides για .NET

Το Aspose.Slides για .NET είναι μια ισχυρή βιβλιοθήκη που σας επιτρέπει να εργάζεστε με παρουσιάσεις PowerPoint μέσω προγραμματισμού. Δείτε πώς μπορείτε να την προσθέσετε στο έργο σας:

### Εγκατάσταση

**.NET CLI:**
```bash
dotnet add package Aspose.Slides
```

**Κονσόλα Διαχείρισης Πακέτων:**
```powershell
Install-Package Aspose.Slides
```

**Διεπαφή χρήστη του διαχειριστή πακέτων NuGet:** 
- Ανοίξτε το NuGet Package Manager και αναζητήστε το "Aspose.Slides". Εγκαταστήστε την πιο πρόσφατη έκδοση.

### Απόκτηση Άδειας

Για να αξιοποιήσετε πλήρως το Aspose.Slides, ξεκινήστε με μια δωρεάν δοκιμαστική περίοδο. Για εκτεταμένη χρήση, εξετάστε το ενδεχόμενο να αποκτήσετε μια προσωρινή άδεια χρήσης ή να αγοράσετε μία από [Άσποζε](https://purchase.aspose.com/buy)Ακολουθήστε τις οδηγίες τους για να ρυθμίσετε την άδεια χρήσης σας στο περιβάλλον του έργου σας.

### Βασική Αρχικοποίηση

Μόλις εγκατασταθεί, αρχικοποιήστε το `Presentation` τάξη για να ξεκινήσει η δημιουργία διαφανειών:
```csharp
using Aspose.Slides;

// Αρχικοποίηση παρουσίας κλάσης παρουσίασης
tPresentation presentation = new Presentation();
```

Τώρα, ας προχωρήσουμε στην υλοποίηση της λειτουργίας δημιουργίας και προσαρμογής γραφημάτων χρησιμοποιώντας το Aspose.Slides για .NET.

## Οδηγός Εφαρμογής

### Δημιουργήστε ένα γράφημα με στοίβες στηλών

Στόχος μας είναι να δημιουργήσουμε ένα γράφημα σωρευμένων στηλών και να το προσαρμόσουμε εμφανίζοντας ποσοστά ως ετικέτες δεδομένων. Δείτε πώς:

#### Αρχικοποίηση της παρουσίασης

Ξεκινήστε δημιουργώντας μια παρουσία του `Presentation`:
```csharp
using Aspose.Slides;

// Αρχικοποίηση παρουσίας κλάσης παρουσίασης
tPresentation presentation = new Presentation();
ISlide slide = presentation.Slides[0];
```

#### Προσθήκη γραφήματος στη διαφάνεια

Προσθέστε ένα γράφημα σωρευμένων στηλών στην πρώτη σας διαφάνεια σε καθορισμένες συντεταγμένες και διαστάσεις:
```csharp
IChart chart = slide.Shapes.AddChart(ChartType.StackedColumn, 20, 20, 400, 400);
```
Αυτή η γραμμή δημιουργεί ένα `StackedColumn` διάγραμμα στη θέση (20, 20) με πλάτος και ύψος 400.

#### Υπολογισμός συνολικών τιμών για υπολογισμό ποσοστού

Για να εμφανίσετε ποσοστά, υπολογίστε τη συνολική τιμή για κάθε κατηγορία σε όλες τις σειρές:
```csharp
IChartSeries series;
double[] total_for_Cat = new double[chart.ChartData.Categories.Count];

for (int k = 0; k < chart.ChartData.Categories.Count; k++)
{
    IChartCategory cat = chart.ChartData.Categories[k];
    // Άθροιση τιμών όλων των σειρών για κάθε κατηγορία
    for (int i = 0; i < chart.ChartData.Series.Count; i++)
    {
        total_for_Cat[k] += Convert.ToDouble(chart.ChartData.Series[i].DataPoints[k].Value.Data);
    }
}
```

#### Προσαρμόστε τις ετικέτες δεδομένων για να εμφανίζουν ποσοστιαίες τιμές

Στη συνέχεια, επαναλάβετε κάθε σειρά και προσαρμόστε τις ετικέτες δεδομένων:
```csharp
for (int x = 0; x < chart.ChartData.Series.Count; x++)
{
    series = chart.ChartData.Series[x];
    series.Labels.DefaultDataLabelFormat.ShowLegendKey = false;

    for (int j = 0; j < series.DataPoints.Count; j++)
    {
        IDataLabel lbl = series.DataPoints[j].Label;
        
        // Υπολογισμός ποσοστού
        double dataPontPercent = (Convert.ToDouble(series.DataPoints[j].Value.Data) / total_for_Cat[j]) * 100;
        IPortion port = new Portion();
        port.Text = String.Format("{0:F2} %", dataPontPercent);
        port.PortionFormat.FontHeight = 8f;

        lbl.TextFrameForOverriding.Text = ""; // Καθαρό κείμενο για αποφυγή επικαλύψεων
        IParagraph para = lbl.TextFrameForOverriding.Paragraphs[0];
        para.Portions.Add(port);

        // Ρύθμιση παραμέτρων μορφής ετικέτας για απόκρυψη προεπιλεγμένων ετικετών δεδομένων
        lbl.DataLabelFormat.ShowSeriesName = false;
        lbl.DataLabelFormat.ShowPercentage = false; 
        lbl.DataLabelFormat.ShowLegendKey = false;
        lbl.DataLabelFormat.ShowCategoryName = false;
        lbl.DataLabelFormat.ShowBubbleSize = false;
    }
}
```

Αυτή η ενότητα υπολογίζει το ποσοστό για κάθε σημείο δεδομένων και το ορίζει ως προσαρμοσμένη ετικέτα, διασφαλίζοντας ότι δεν θα υπάρχει επικάλυψη με τις προεπιλεγμένες ετικέτες.

#### Αποθήκευση της παρουσίασης

Τέλος, αποθηκεύστε την παρουσίασή σας για να δείτε το αποτέλεσμα:
```csharp
string outputDir = "YOUR_OUTPUT_DIRECTORY";
presentation.Save(outputDir + "/DisplayPercentageAsLabels_out.pptx", SaveFormat.Pptx);
```

## Πρακτικές Εφαρμογές

Η εμφάνιση ποσοστών σε γραφήματα μπορεί να είναι ιδιαίτερα χρήσιμη σε σενάρια όπως:
1. **Οικονομική Αναφορά:** Εμφάνιση κατανομών χαρτοφυλακίου ή αποδόσεων επενδύσεων ως ποσοστά.
2. **Ανάλυση Πωλήσεων:** Αναπαραστήστε τα δεδομένα μεριδίου αγοράς με βάση το ποσοστό για να επισημάνετε την απόδοση σε όλες τις περιοχές.
3. **Αποτελέσματα Έρευνας:** Εμφάνιση απαντήσεων έρευνας ως ποσοστά για καλύτερη οπτική σύγκριση.
4. **Διαχείριση Έργου:** Χρησιμοποιήστε κυκλικά διαγράμματα με ποσοστά για να απεικονίσετε την κατανομή των πόρων.
5. **Εκπαίδευση:** Εξηγήστε στατιστικές έννοιες χρησιμοποιώντας σαφείς απεικονίσεις που βασίζονται σε ποσοστά.

Η ενσωμάτωση αυτών των προσαρμοσμένων γραφημάτων σε συστήματα όπως το CRM ή το ERP μπορεί να βελτιώσει τους πίνακες ελέγχου και τις αναφορές, βοηθώντας τις διαδικασίες λήψης αποφάσεων.

## Παράγοντες Απόδοσης

Όταν εργάζεστε με το Aspose.Slides για .NET, ειδικά με μεγάλα σύνολα δεδομένων:
- **Διαχείριση μνήμης:** Απορρίψτε σωστά τα αντικείμενα παρουσίασης για να ελευθερώσετε χώρο στη μνήμη. Χρησιμοποιήστε `using` δηλώσεις όπου εφαρμόζεται.
- **Αποτελεσματική διαχείριση δεδομένων:** Εκτελέστε υπολογισμούς εκτός βρόχων όταν είναι δυνατόν για να μειώσετε την υπολογιστική επιβάρυνση.
- **Εξισορρόπηση φορτίου:** Για εφαρμογές web, βεβαιωθείτε ότι οι πόροι του διακομιστή έχουν προβλεφθεί επαρκώς για ταυτόχρονα αιτήματα δημιουργίας γραφημάτων.

## Σύναψη

Αυτό το σεμινάριο κάλυψε τη δημιουργία και την προσαρμογή γραφημάτων χρησιμοποιώντας το Aspose.Slides για .NET, εμφανίζοντας ποσοστιαίες τιμές ως ετικέτες. Η εξειδίκευση σε αυτές τις τεχνικές σάς επιτρέπει να βελτιώσετε τις παρουσιάσεις σας με λεπτομερείς και οπτικά ελκυστικές αναπαραστάσεις δεδομένων.

Ως επόμενο βήμα, εξερευνήστε άλλους τύπους γραφημάτων και επιλογές προσαρμογής που είναι διαθέσιμες στο Aspose.Slides. Πειραματιστείτε με διαφορετικά σύνολα δεδομένων για να τα μετατρέψετε σε ισχυρά γραφήματα που μεταδίδουν με σαφήνεια πληροφορίες.

## Ενότητα Συχνών Ερωτήσεων

**Ε1: Πώς μπορώ να χειριστώ μεγάλα σύνολα δεδομένων κατά τη δημιουργία γραφημάτων με το Aspose.Slides για .NET;**
A1: Για μεγάλα σύνολα δεδομένων, βελτιστοποιήστε τους υπολογισμούς και χρησιμοποιήστε αποτελεσματικές τεχνικές διαχείρισης μνήμης. Αναλύστε τις εργασίες επεξεργασίας για να αποφύγετε την υπερφόρτωση μνήμης.

**Ε2: Μπορώ να χρησιμοποιήσω το Aspose.Slides για .NET σε μια εφαρμογή web;**
A2: Ναι, μπορεί να ενσωματωθεί σε εφαρμογές ASP.NET. Διασφαλίστε την σωστή κατανομή πόρων διακομιστή για βέλτιστη απόδοση.

**Ε3: Είναι δυνατή η εξαγωγή γραφημάτων που δημιουργήθηκαν με το Aspose.Slides σε άλλες μορφές;**
A3: Απολύτως! Μπορείτε να εξαγάγετε παρουσιάσεις που περιέχουν τα προσαρμοσμένα γραφήματα σας σε διάφορες μορφές, όπως αρχεία PDF και εικόνας, χρησιμοποιώντας τις δυνατότητες της βιβλιοθήκης.

{{< /blocks/products/pf/tutorial-page-section >}}

{{< /blocks/products/pf/main-container >}}

{{< /blocks/products/pf/main-wrap-class >}}

{{< blocks/products/products-backtop-button >}}